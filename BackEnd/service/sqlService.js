const constants = require('../constants');
const { dbHelper } = require('../helper/dbHelper');
const { sqlConnection } = require('../database/connection')


const sqlConn = sqlConnection();

module.exports.getUserData = async ( reData ) => {
  
}